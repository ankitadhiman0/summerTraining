import math
num = 46.678
print(math.floor(num)) # round a number down to the nearest integer
print(math.sqrt(num)) # return the square of number
print(math.pow(num,2)) # return power
print(math.ceil(num)) # rounds a number nearest up to the integer
print(math.factorial(3)) # returns the factorial of number